# DAF — Design Automation Framework

AI-orchestrated pipeline that turns a brand interview into a complete, validated design system package on disk.

## Install

```bash
pip install -e ".[dev]"
```

## Usage

```bash
daf init ./my-design-system
```
